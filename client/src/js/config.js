const config = {
  paths: {
    src: "./src",
    dist: "./dist"
  },
  localServer: {
    port: 8001,
    url: "http://localhost:8001/"
  },
  apiServer: {
    url: "http://localhost:8000/"
  }
};

module.exports = config;
